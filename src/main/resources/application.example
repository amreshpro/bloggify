server:
  port: 8080
spring:
  application:
    name: blogging
  data:
    mongodb:
     uri:
     auto-index-creation: true
  security:
    filter:
      order: 10
    user:
      name: amresh
      password: amreshpro

springdoc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html
      configUrl: /v3/api-docs/swagger-config
